print ("Hello")

# git status  ---- It will check the status of the file changes

# git add filename -------it will add the file to the staging repo

# git commit -m "commit message"  -- it will identify the code change/versioning 

# git push    --- it will  push the code to the repo